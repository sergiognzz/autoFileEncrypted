# autoFileEncrypted
autoEncrypt.sh is a Bash script that allows secure encryption and decryption of files using the AES-256-CBC algorithm via OpenSSL. It prompts the user for a password interactively and is ideal for learning basic cryptography, automation tasks, and CTF environments.
